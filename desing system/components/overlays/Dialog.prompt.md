Modal centralizado. Clique fora fecha (`onClose`).

```jsx
<Dialog open={true} title="Remover item?" onClose={() => {}} footer={<Button variant="primary">Remover</Button>}>
  Esta ação não pode ser desfeita.
</Dialog>
```
